{
  "type": "object",
  "properties": {
    "intent": {
      "type": "string",
      "enum": ["conversation", "action", "memory_update"]
    },
    "summary": {
      "type": "string",
      "description": "A 15-word recap containing specific keywords and context from the most recent text."
    }
  },
  "required": ["intent", "summary"],
  "additionalProperties": false
}